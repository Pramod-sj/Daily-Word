<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.pramod.dailyword.framework.util.CalenderUtil" />

        <import type="com.pramod.dailyword.business.domain.model.Word" />

        <variable
            name="word"
            type="Word" />

    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_small"
        android:transitionName="@{word.date}"
        app:cardCornerRadius="25dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/padding_large">

            <TextView
                style="@style/AppTheme.SmallTextStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{CalenderUtil.getFancyDay(word.date,CalenderUtil.DATE_FORMAT)}" />

            <com.google.android.material.textview.MaterialTextView
                android:id="@+id/textView_item_word"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:drawableEnd="@{word.seen?null:@drawable/background_circle_12dp}"
                android:drawablePadding="@dimen/margin_medium"
                android:text="@{word.word}"
                android:textSize="@dimen/text_h5"
                android:textStyle="bold" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>
